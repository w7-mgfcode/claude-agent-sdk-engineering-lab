# CLAUDE.md

@AGENTS.md

## Claude-specific notes

- Runtime configuration (permissions, hooks, MCP) is managed in `.claude/` or via application options — canonical policy stays in `AGENTS.md`.
- Read `AGENTS.md` for project mission, setup, verification gates, and code style rules.
